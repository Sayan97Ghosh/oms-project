// import { FastifyPluginAsync } from 'fastify';

// const buildApp: FastifyPluginAsync = async (f) => {
//   // f.register(dbPlugin);
//   // f.register(authPlugin);
//   // f.register(orderRoutes, { prefix: '/orders' });
// };

// export default buildApp;
